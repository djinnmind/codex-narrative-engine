{
  "name": "codex-ide",
  "private": true,
  "workspaces": [
    "packages/codex-core",
    "packages/codex-obsidian",
    "packages/cloud-contract"
  ],
  "scripts": {
    "build": "npm run build:contract && npm run build:core && npm run build:obsidian",
    "build:contract": "cd packages/cloud-contract && npm run build",
    "build:core": "cd packages/codex-core && npm run build",
    "build:obsidian": "cd packages/codex-obsidian && npm run build",
    "test": "cd packages/codex-core && npm run test",
    "dev": "cd packages/codex-obsidian && npm run dev"
  }
}
